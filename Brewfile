cask_args appdir: '/Applications'

brew 'ack'
brew 'b2-tools'
brew 'cloudflared'          # bin/cf-tunnel-up and cf-tunnel-down.
brew 'composer'
brew 'coreutils'            # gls for system/aliases.zsh, gdate for bin/ssh-timer.
brew 'dnsmasq'              # Valet.
brew 'gh'
brew 'git'
brew 'git-delta'            # git pager, see git/gitconfig.symlink.
brew 'go'
brew 'grc'                  # Needed for `ls` to work in ZSH apparently?
brew 'imagemagick'
brew 'llm'
brew 'libmemcached'
brew 'mailpit'
brew 'mariadb'
brew 'memcached'
brew 'mkcert'               # Local dev certificates.
brew 'mole'
brew 'nginx'                # Valet.
brew 'nss'                  # Installs certutil, which is able to install CAs in Firefox.
brew 'openssl'
brew 'php@8.4'              # Valet.
brew 'python'
brew 'ripgrep'
brew 'rsync'
brew 'subversion'
brew 'tree'
brew 'uv'
brew 'visidata'
brew 'wget'
brew 'wp-cli'               # Depends on unversioned `php`. Valet force-links php@8.4 over it, so
                            # keep `php` pinned or an upgrade relinks 8.5 and Valet's php vanishes.
brew 'yt-dlp'               # bin/getmusic.

cask 'bruno'
cask 'ghostty'
cask 'ngrok'
